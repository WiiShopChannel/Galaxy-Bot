
local settings
settings = {
  prefix = "$", -- change this to change the bot's prefix.
  bot_version = "v0.1A", -- this doesn't do anything unless you want it to.
  debug_mode = false, -- makes it so only the bot owner can use commands.
}
return settings
